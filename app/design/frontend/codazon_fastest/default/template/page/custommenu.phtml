<div class="cdz-main-menu cdz-main-menu-horizontal">
<ul class="groupmenu">

    <?php
        $obj = new Mage_Catalog_Block_Navigation();
        $storeCategories = $obj->getStoreCategories();
        Mage::registry('current_category') ? $currentCategoryId = Mage::registry('current_category')->getId() : $currentCategoryId='';
        foreach ($storeCategories as $_category):
    ?>
           <li class="level0 level-top parent">
		   <span class="cdz-menu-toggle">toggle</span>
<a class="menu-link" href="<?php echo $_category->getUrl(); ?>"><span><?php echo $_category->getName(); ?></span></a>
		   <div class="groupmenu-drop">
<div class="groupmenu-drop-content groupmenu-6" style="padding-bottom: 60px;">
<div class="row">
                
                <?php $categoryChildren = $_category->getChildren(); ?>
                <?php if($categoryChildren->count()) : ?>
                    
 <div class="col-sm-4">
                        <?php foreach($categoryChildren as $_categoryChild) : ?>
                            <?php $_categoryChildModel = Mage::getModel('catalog/category')->load($_categoryChild->getId());?>
							<div class="groupdrop-banner space-s">
							<img class="img-responsive" src="<?php echo Mage::getBaseUrl('media') . 'catalog/category/' .Mage::getModel('catalog/category')->load($_categoryChild->getId())->getThumbnail();?>"/>
							</div>
							
							
                            <?php $categoryGrandchildren=$_categoryChild->getChildren(); ?>
                           
                          <?php
						  if($currentCategoryId===$_categoryChild->getId()){?>
							  <p class="groupdrop-title">
							  
<a href="<?php echo $_categoryChildModel->getUrl() ?>">
<?php echo $_categoryChild->getName() ?></a>
</p>
							  
						  <?php }	?>				  
                      

							   
                           <ul class="groupdrop-link">
                            <?php if($categoryGrandchildren->count()) : ?>
                                <?php foreach($categoryGrandchildren as $_categoryGrandchild) : ?>
                                    <?php $_categoryGrandchildModel = Mage::getModel('catalog/category')->load($_categoryGrandchild->getId());?>
                                   
                                        <?php
                                            $currentCategoryId===$_categoryChild->getId() ;
                                            echo '&emsp;&emsp;' . '<a href="' . $_categoryGrandchildModel->getUrl() . '"' . $bold . '>' .  $_categoryGrandchild->getName().'</a>';
                                        ?>
                                   
                                <?php endforeach; ?>
                            <?php endif; ?>
                        <?php endforeach; ?>
                   
                <?php endif; ?>
           
        <?php endforeach ?>
		</div><!--row div-->
</div>
</div> 
</ul>
</div>